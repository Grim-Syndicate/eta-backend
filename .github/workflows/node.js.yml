name: Dev CI

on:
  push:
    branches: [ dev ]

jobs:
  deployment:
    runs-on: self-hosted
    steps:
      - name: Checkout branch
        uses: actions/checkout@v3

      - name: Install dependencies
        run: su nodejs npm ci

      - name: Restart server application
        run: su nodejs pm2 restart ETA
